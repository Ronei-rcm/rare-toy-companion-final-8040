═══════════════════════════════════════════════════════════════════════════════
  📜 SCRIPTS DE INSTALAÇÃO - MUHLSTORE
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│                         🚀 INSTALAÇÃO AUTOMÁTICA                            │
└─────────────────────────────────────────────────────────────────────────────┘

Script: install.sh
Uso:    sudo ./install.sh
Tempo:  10-20 minutos

O QUE FAZ:
  ✅ Instala Node.js (via NVM)
  ✅ Instala PM2
  ✅ Instala MySQL (Docker ou nativo - você escolhe)
  ✅ Configura Nginx + SSL (Let's Encrypt)
  ✅ Faz build e deploy da aplicação
  ✅ Configura firewall
  ✅ Cria backup automático diário

REQUERIMENTOS:
  • Ubuntu/Debian/CentOS/Rocky Linux
  • Acesso root (sudo)
  • Domínio configurado
  • Internet

EXEMPLO:
  wget https://raw.githubusercontent.com/SEU_REPO/main/install.sh
  chmod +x install.sh
  sudo ./install.sh

┌─────────────────────────────────────────────────────────────────────────────┐
│                         🗑️  DESINSTALAÇÃO COMPLETA                          │
└─────────────────────────────────────────────────────────────────────────────┘

Script: uninstall.sh
Uso:    sudo ./uninstall.sh

O QUE FAZ:
  ✅ Cria backup antes de remover (opcional)
  ✅ Para processos PM2
  ✅ Remove configuração Nginx
  ✅ Remove SSL
  ✅ Remove MySQL (Docker)
  ✅ Remove arquivos do projeto
  ✅ Opcionalmente remove Node/Docker/Nginx

⚠️  ATENÇÃO: Ação irreversível! Sempre faça backup.

┌─────────────────────────────────────────────────────────────────────────────┐
│                           💾 BACKUP AUTOMÁTICO                              │
└─────────────────────────────────────────────────────────────────────────────┘

Script: backup.sh
Uso:    ./backup.sh

O QUE FAZ:
  ✅ Cria ZIP com código-fonte
  ✅ Exporta dump do MySQL
  ✅ Mantém últimos 7 backups
  ✅ Remove backups antigos

AGENDAMENTO:
  • Criado automaticamente pelo install.sh
  • Executa diariamente às 3h da manhã
  • Via crontab

LOCAL DOS BACKUPS:
  ./backups/backup_YYYY-MM-DD_HH-MM-SS.zip

┌─────────────────────────────────────────────────────────────────────────────┐
│                        📝 COMANDOS RÁPIDOS PM2                              │
└─────────────────────────────────────────────────────────────────────────────┘

  pm2 status              # Ver status de todos os processos
  pm2 logs                # Ver logs em tempo real
  pm2 logs api            # Ver logs da API
  pm2 restart all         # Reiniciar todos
  pm2 restart api         # Reiniciar API
  pm2 monit               # Monitoramento visual
  pm2 save                # Salvar configuração

┌─────────────────────────────────────────────────────────────────────────────┐
│                       🔧 COMANDOS RÁPIDOS NGINX                             │
└─────────────────────────────────────────────────────────────────────────────┘

  sudo nginx -t                                    # Testar config
  sudo systemctl reload nginx                      # Recarregar
  sudo systemctl restart nginx                     # Reiniciar
  sudo tail -f /var/log/nginx/muhlstore-error.log  # Ver erros

┌─────────────────────────────────────────────────────────────────────────────┐
│                      🐳 COMANDOS RÁPIDOS MYSQL (DOCKER)                     │
└─────────────────────────────────────────────────────────────────────────────┘

  docker ps | grep mysql                          # Ver status
  docker logs muhlstore-mysql                     # Ver logs
  docker exec -it muhlstore-mysql mysql -u root -p  # Console MySQL
  docker-compose up -d                            # Iniciar
  docker-compose down                             # Parar

┌─────────────────────────────────────────────────────────────────────────────┐
│                         🔄 FLUXO DE ATUALIZAÇÃO                             │
└─────────────────────────────────────────────────────────────────────────────┘

  1. Fazer backup:
     ./backup.sh

  2. Atualizar código:
     git pull

  3. Instalar dependências:
     npm install

  4. Build:
     npm run build

  5. Reiniciar:
     pm2 restart all

┌─────────────────────────────────────────────────────────────────────────────┐
│                       📁 ESTRUTURA DE ARQUIVOS                              │
└─────────────────────────────────────────────────────────────────────────────┘

  install.sh         → Script de instalação automática
  uninstall.sh       → Script de desinstalação
  backup.sh          → Script de backup (criado pelo install.sh)
  
  INSTALL.md         → Guia detalhado de instalação
  SCRIPTS.md         → Referência completa de todos os scripts
  README.md          → Documentação principal
  MANUAL_WHATSAPP.md → Configuração WhatsApp
  
  ecosystem.config.cjs  → Configuração PM2
  package.json          → Scripts npm disponíveis

┌─────────────────────────────────────────────────────────────────────────────┐
│                         📚 DOCUMENTAÇÃO COMPLETA                            │
└─────────────────────────────────────────────────────────────────────────────┘

  README.md       → Visão geral do projeto
  INSTALL.md      → Guia de instalação passo a passo
  SCRIPTS.md      → Referência de todos os scripts
  MANUAL_WHATSAPP.md → Configuração WhatsApp Business

┌─────────────────────────────────────────────────────────────────────────────┐
│                          🆘 TROUBLESHOOTING                                 │
└─────────────────────────────────────────────────────────────────────────────┘

  Problema: Site não carrega
  → pm2 status
  → pm2 logs
  → sudo systemctl status nginx

  Problema: Erro 502
  → pm2 restart api
  → pm2 logs api

  Problema: MySQL não conecta
  → docker ps | grep mysql
  → docker logs muhlstore-mysql

  Problema: SSL não funciona
  → sudo certbot renew
  → sudo ufw status

┌─────────────────────────────────────────────────────────────────────────────┐
│                        ⚡ INSTALAÇÃO RÁPIDA                                 │
└─────────────────────────────────────────────────────────────────────────────┘

  # Em um servidor Linux limpo, execute:
  
  wget https://raw.githubusercontent.com/SEU_REPO/main/install.sh
  chmod +x install.sh
  sudo ./install.sh
  
  # Responda às perguntas
  # Aguarde 10-20 minutos
  # Pronto! Acesse: https://seu-dominio.com.br

═══════════════════════════════════════════════════════════════════════════════
  Desenvolvido com ❤️  para facilitar deploy em produção
═══════════════════════════════════════════════════════════════════════════════

