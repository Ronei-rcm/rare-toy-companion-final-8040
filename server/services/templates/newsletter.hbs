<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Newsletter MuhlStore</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            background-color: #f8f9fa;
        }
        .container {
            background: white;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px 20px;
            text-align: center;
        }
        .header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: bold;
        }
        .header p {
            margin: 10px 0 0 0;
            opacity: 0.9;
        }
        .content {
            padding: 30px 20px;
        }
        .greeting {
            font-size: 18px;
            margin-bottom: 20px;
            color: #2c3e50;
        }
        .section {
            margin-bottom: 30px;
        }
        .section h2 {
            color: #667eea;
            border-bottom: 2px solid #667eea;
            padding-bottom: 10px;
            margin-bottom: 20px;
        }
        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 20px;
            margin-bottom: 20px;
        }
        .product-card {
            border: 1px solid #e0e0e0;
            border-radius: 8px;
            padding: 15px;
            text-align: center;
            transition: transform 0.2s;
        }
        .product-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        .product-image {
            width: 100%;
            height: 120px;
            object-fit: cover;
            border-radius: 5px;
            margin-bottom: 10px;
        }
        .product-name {
            font-weight: bold;
            margin-bottom: 5px;
            color: #2c3e50;
        }
        .product-price {
            color: #e74c3c;
            font-weight: bold;
            font-size: 16px;
        }
        .cta-button {
            display: inline-block;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 12px 30px;
            text-decoration: none;
            border-radius: 25px;
            font-weight: bold;
            margin: 20px 0;
            transition: transform 0.2s;
        }
        .cta-button:hover {
            transform: translateY(-2px);
        }
        .offers {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            margin: 20px 0;
        }
        .offer-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 0;
            border-bottom: 1px solid #e0e0e0;
        }
        .offer-item:last-child {
            border-bottom: none;
        }
        .footer {
            background: #2c3e50;
            color: white;
            padding: 20px;
            text-align: center;
            font-size: 14px;
        }
        .footer a {
            color: #3498db;
            text-decoration: none;
        }
        .unsubscribe {
            margin-top: 15px;
            font-size: 12px;
            opacity: 0.8;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🎉 MuhlStore</h1>
            <p>Suas novidades e ofertas especiais</p>
        </div>
        
        <div class="content">
            <div class="greeting">
                Olá, <strong>{{customerName}}</strong>! 👋
            </div>
            
            <div class="section">
                <h2>🛍️ Produtos em Destaque</h2>
                <div class="products-grid">
                    {{#each products}}
                    <div class="product-card">
                        <img src="{{imagemUrl}}" alt="{{nome}}" class="product-image">
                        <div class="product-name">{{nome}}</div>
                        <div class="product-price">R$ {{preco}}</div>
                    </div>
                    {{/each}}
                </div>
                <div style="text-align: center;">
                    <a href="{{shopLink}}" class="cta-button">Ver Todos os Produtos</a>
                </div>
            </div>
            
            {{#if offers.length}}
            <div class="section">
                <h2>🎟️ Ofertas Especiais</h2>
                <div class="offers">
                    {{#each offers}}
                    <div class="offer-item">
                        <span><strong>{{title}}</strong></span>
                        <span style="color: #e74c3c; font-weight: bold;">{{discount}}% OFF</span>
                    </div>
                    {{/each}}
                </div>
            </div>
            {{/if}}
            
            <div class="section">
                <h2>💡 Dica da Semana</h2>
                <p>Não perca nossas promoções relâmpago! Siga-nos nas redes sociais e ative as notificações para ser o primeiro a saber das melhores ofertas.</p>
            </div>
        </div>
        
        <div class="footer">
            <p><strong>MuhlStore</strong> - Sua loja de brinquedos favorita!</p>
            <p>📧 {{currentDate}} | 🌐 <a href="{{shopLink}}">Visite nossa loja</a></p>
            <div class="unsubscribe">
                <a href="{{unsubscribeLink}}">Cancelar inscrição</a>
            </div>
        </div>
    </div>
</body>
</html>
